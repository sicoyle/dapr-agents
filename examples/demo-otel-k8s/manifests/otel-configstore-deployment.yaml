apiVersion: apps/v1
kind: Deployment
metadata:
  name: otel-configstore
  labels:
    app: otel-configstore
spec:
  replicas: 1
  selector:
    matchLabels:
      app: otel-configstore
  template:
    metadata:
      labels:
        app: otel-configstore
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "otel-configstore"
        dapr.io/app-port: "8080"
        dapr.io/enable-api-logging: "true"
    spec:
      containers:
      - name: agent
        image: localhost:5001/demo-otel-agent:latest
        command: ["python", "/app/agents/configstore/app.py"]
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
