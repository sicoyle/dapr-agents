[flake8]
# Comma-separated list of errors/warnings to ignore
ignore = E501,F401,W503,E203

# Comma-separated list of directories to exclude from checks
exclude =
    .git,
    __pycache__,
    build,
    dist,
    venv,
    .tox